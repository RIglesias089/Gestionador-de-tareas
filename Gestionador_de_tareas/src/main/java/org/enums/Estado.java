package org.enums;

//Asi como con el enum de prioridad hacemos este enum con valores constantes que conocemos para poder
//poder saber si la tarea en un estado especifico
public enum Estado {
    PENDIENTE,
    EN_PROGRESO,
    COMPLETADO,
    CANCELADA
}
